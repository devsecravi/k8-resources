apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    # labels are used to selectors
    project: roboshop
    component: frontend
    environment: dev
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
